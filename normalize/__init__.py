# This package directory is intentionally empty.
#
# The normalize logic lives in:
#   processor/normalize.py  - raw_items → items deduplication and extraction
#
# This directory was retained for structural consistency with the package layout.
# Do NOT add implementation code here; use processor/ instead.
