/**
 * Root-level module helpers (e.g. imports from `../utils` under `src/modules/`).
 * Re-exports from `render/utils` so shared symbols stay single-sourced.
 */
export { getDomainSlugClass } from './render/utils';
